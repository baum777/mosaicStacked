node_modules/
dist/
coverage/
test-results/
.env
.env.local
.env.router
.local-ai/
router.local.yml
llm-router.local.yml
.DS_Store
*.log
server/node_modules/
server/dist/
web/node_modules/
web/dist/

# Local perf cache lives at web/src/lib/perf-cache.json. The stub with
# the initial "unknown" value IS committed (provides first-build
# determinism). After commit, the file is marked skip-worktree so
# scripts/check-web-bundle-budget.mjs and scripts/check-lighthouse-tti.mjs
# can overwrite it locally without producing commit noise.

.vercel
update-vercel-env.sh
.env*.local

*.pem
